"""MediKiosk clinical ontology package.

Public API (implemented in T1.2): from .loader import load_ontology
Kept non-importing here so T1.2's RED test fails for the right reason
(loader module missing) rather than a package-resolution error.
"""
