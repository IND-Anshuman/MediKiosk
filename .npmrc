# pnpm + Next.js standalone fix: Next's standalone output traces node_modules,
# and pnpm's default symlinked store produces broken copies inside
# .next/standalone (next ends up without package.json -> "Cannot find module
# 'next'" at container start). A hoisted (flat) node_modules is required.
node-linker=hoisted